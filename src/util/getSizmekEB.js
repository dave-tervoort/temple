import loadScript from './loadScript';

let prom = null;
let promInitialized = null;
const limit = 10000;
let count = 0;

/**
 * Will return the Enabler initialized and well.
 * @param {boolean} isInitialized
 * @return {*}
 */
export default function getSizmekEB() {}
